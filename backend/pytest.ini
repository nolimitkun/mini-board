[pytest]
testpaths = tests
# Legacy manual scripts are not hermetic unit tests: test_api.py needs a
# running server and test_duckdb.py needs local v7 CSVs. Exclude them so the
# suite stays runnable in CI.
addopts = -q --ignore=tests/test_api.py --ignore=tests/test_duckdb.py
